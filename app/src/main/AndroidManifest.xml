<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="www.mp5a5.com.materialdesignkotlin">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.act.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.act.BeautifulGirlDetailActivity" />
        <activity android:name=".view.act.TabScrollViewActivity" />
        <activity android:name=".view.frag.AliPayActivity" />
        <activity android:name=".view.act.BottomSheetActivity" />
        <activity android:name=".view.act.BottomSheetDialogActivity" />
        <activity android:name=".view.act.BottomSheetDialogFragmentAct" />
    </application>

</manifest>